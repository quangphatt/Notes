import { View } from 'react-native';
import RootNavigation from '@/navigation/RootNavigation';

const App = () => {
  return (
    <View style={{ flex: 1 }}>
      <RootNavigation />
    </View>
  );
};

export default App;
